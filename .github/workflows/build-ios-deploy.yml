name: build-ios
on:
  push:
    branches:
      - 'ios'

  workflow_dispatch:
jobs:
  deploy:
    runs-on: macos-latest

     permissions:
      contents: write

    steps:
        uses: actions/checkout@v2
      - run: fastlane beta
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}